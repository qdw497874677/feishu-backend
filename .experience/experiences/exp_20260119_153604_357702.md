问题：FeishuGatewayImpl.sendReply 方法中同时调用了 .message() 和 .content()，导致参数冲突和 SDK 调用失败。解决方案：只使用 .content() 方法传递 MessageText 对象。删除 .message() 调用。注意：飞书 SDK 的 MessageText 需要通过 MessageText.newBuilder().text(内容).build() 构建。不要混用不同的消息设置方式。