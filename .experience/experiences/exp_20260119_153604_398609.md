问题：feishu-bot-start 模块缺少 feishu-bot-adapter 依赖，导致 FeishuWebhookController 类无法被扫描和加载。解决方案：在 feishu-bot-start/pom.xml 中添加所有必要的模块依赖（client, domain, app, infrastructure, adapter）。注意：对于 Web 层的 controller，需要确保其所在的模块被包含在启动模块的依赖中，Spring 才能扫描到 @RestController 和 @Component。