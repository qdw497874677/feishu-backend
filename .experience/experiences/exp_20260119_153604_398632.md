经验：将机器人的回复逻辑从 Echo（你说：xxx）优化为纯回显（直接返回 xxx）。原因：1. 避免重复和困惑；2. 更简洁清晰；3. 为未来添加复杂业务逻辑预留空间。实现：在 Message.generateReply() 方法中直接返回 this.content。注意：如果未来需要添加智能回复（如调用 AI API），可以在 BotMessageService.handleMessage 中处理，Message.generateReply 只负责基本格式转换。