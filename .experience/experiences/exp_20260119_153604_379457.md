问题：领域服务（如 BotMessageService）未注册为 Spring Bean，导致启动时 'No qualifying bean' 错误。解决方案：方式1 - 在领域服务类上添加 @Service 注解；方式2 - 创建 @Configuration 配置类，使用 @Bean 方法显式注册。建议：优先使用 @Service 注解，更简单清晰。注意：在 COLA 架构中，领域层的类也需要注册为 Spring Bean 才能被应用层和基础设施层注入使用。