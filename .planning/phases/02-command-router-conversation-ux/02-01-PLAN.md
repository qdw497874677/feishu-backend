---
phase: 02-command-router-conversation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeTaskExecutor.java
  - feishu-bot-app/src/main/java/com/qdw/feishu/app/message/BotMessageAppService.java
  - feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeApp.java
  - feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java
  - feishu-bot-domain/src/main/java/com/qdw/feishu/domain/topic/TopicCommandValidator.java
  - feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandlerTest.java
  - feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/OpenCodeAppTest.java
  - feishu-bot-app/src/test/java/com/qdw/feishu/app/message/BotMessageAppServiceTest.java
autonomous: true
requirements: [CMD-01, CMD-02, UX-02, COMPAT-02]

must_haves:
  truths:
    - "异步任务（chat/chatnow/new 的执行路径）不产生空消息气泡"
    - "用户在话题内输入 /oc status 能看到当前绑定状态"
    - "所有状态白名单包含完整别名（p/s/cn 等）"
    - "群聊非话题中执行 chat/sc/session 时，错误消息包含 '请在话题中操作' 和具体引导"
    - "280 个现有测试继续通过"
  artifacts:
    - path: "feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeTaskExecutor.java"
      provides: "异步执行返回 noReply()"
      contains: "AppExecutionResult.noReply()"
    - path: "feishu-bot-app/src/main/java/com/qdw/feishu/app/message/BotMessageAppService.java"
      provides: "空字符串防护"
      contains: "replyContent.isEmpty()"
    - path: "feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java"
      provides: "status 命令路由"
      contains: "case \"status\""
    - path: "feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeApp.java"
      provides: "更新后的白名单含 status 和 new"
      contains: "\"status\""
  key_links:
    - from: "OpenCodeCommandHandler.handle()"
      to: "sessionManager.getCurrentSessionStatus()"
      via: "case \"status\" 路由"
      pattern: "case \"status\" -> .*getCurrentSessionStatus"
    - from: "BotMessageAppService.sendReply()"
      to: "ReplyStrategy.reply()"
      via: "空字符串防护拦截"
      pattern: "replyContent\\.isEmpty\\(\\)"
---

<objective>
修复命令路由基础：消除空气泡、新增 status 快捷命令、完善状态白名单和群聊引导消息。

Purpose: 为 Phase 2 的核心 UX 改进（直接输入、下一步提示、状态指示器）打好基础。本计划解决最独立、风险最低的改动。
Output: 空气泡消除 + status 命令 + 白名单别名完整 + 群聊引导
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-command-router-conversation-ux/CONTEXT.md

# Phase 1 基础设施（只读参考，了解 AppExecutionResult 和 MessageContext 的用法）
@.planning/phases/01-context-foundation/01-plan-SUMMARY.md

<interfaces>
<!-- Phase 1 已建立的核心类型，本计划直接使用 -->

From feishu-bot-domain/.../app/AppExecutionResult.java:
```java
public class AppExecutionResult {
    public static AppExecutionResult text(String content);
    public static AppExecutionResult noReply();     // ← UX-02 核心
    public static AppExecutionResult withSession(String content, String openCodeSessionId, boolean created);
    public String getReplyContent();                 // null = 不发送回复
    public String getOpenCodeSessionId();
}
```

From feishu-bot-domain/.../model/MessageContext.java:
```java
public class MessageContext {
    public static MessageContext of(ImContextRef ref, ImContextBinding binding);
    public static MessageContext unresolved();
    public boolean isResolved();
    public boolean isBound();
    public boolean isBoundToApp(String appId);
    public ImContextRef getContextRef();
    public ImContextBinding getBinding();
}
```

From feishu-bot-domain/.../opencode/OpenCodeSessionManager.java:
```java
public TopicState detectTopicState(MessageContext messageContext);
public String getCurrentSessionStatus(MessageContext messageContext);
public Optional<String> getSessionId(MessageContext messageContext);
public boolean isTopicInitialized(MessageContext messageContext);
```

From feishu-bot-domain/.../command/CommandWhitelist.java:
```java
public class CommandWhitelist {
    public static CommandWhitelist all();
    public static Builder builder();
    public boolean isCommandAllowed(String command, TopicState state);
}
// Builder.add(String... commands) 添加到所有状态
```

From feishu-bot-domain/.../topic/TopicCommandValidator.java:
```java
public ValidationResult validateCommand(String subCommand, TopicState state, CommandWhitelist whitelist);
// 内部调用 buildRestrictedMessage(state, command)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 空气泡消除 + 空字符串防护 (UX-02)</name>
  <files>
    feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeTaskExecutor.java,
    feishu-bot-app/src/main/java/com/qdw/feishu/app/message/BotMessageAppService.java,
    feishu-bot-app/src/test/java/com/qdw/feishu/app/message/BotMessageAppServiceTest.java
  </files>
  <behavior>
    - Test: BotMessageAppService.sendReply 收到空字符串 "" 时不调用 ReplyStrategy.reply()，返回 SendResult.success(null)
    - Test: BotMessageAppService.sendReply 收到 null 时不调用 ReplyStrategy.reply()（已有行为，确认不变）
    - Test: BotMessageAppService.sendReply 收到正常文本时正常调用 ReplyStrategy.reply()
    - 验证 OpenCodeTaskExecutor.executeTask() 已经返回 noReply()（Phase 1 已改过，确认不需要改）
  </behavior>
  <action>
    **Per D-03 (UX-02): 空气泡消除**

    1. **BotMessageAppService.sendReply()** — 在现有 `replyContent == null` 检查之后，增加空字符串检查：
       ```java
       if (replyContent == null || replyContent.trim().isEmpty()) {
           log.debug("App {} returned empty content, skipping reply", app.getAppId());
           return SendResult.success(null);
       }
       ```
       这是防护层：即使某处返回了 `""` 或纯空格字符串，也不会发送空气泡。

    2. **OpenCodeTaskExecutor** — 检查确认 `executeTask()` 已返回 `AppExecutionResult.noReply()`（在 Phase 1 Plan 01 中已改过，line 148）。
       排查其他路径：
       - `executeWithAutoSession` → 调用 `executeTask` ✓
       - `executeWithNewSession` → 调用 `executeTask` ✓
       - `executeWithSpecificSession` → 调用 `executeTask` ✓
       - `createSessionOnly` → 返回 String（非异步，返回会话信息，**不需要改**）
       
       如果发现 `executeTask` 以外有返回空字符串的路径，一并修复为 `noReply()`。

    3. **测试** — 在 `BotMessageAppServiceTest` 中新增测试用例：
       - `should_skipReply_when_contentIsEmptyString` — 模拟 app 返回 `AppExecutionResult.text("")`，验证 `ReplyStrategy.reply()` 未被调用
       - `should_skipReply_when_contentIsBlankString` — 模拟 app 返回 `AppExecutionResult.text("  ")`，验证跳过

    **注意**：不修改 `chatnow/cn` 的 `createSessionOnly` 路径——它返回的是会话创建确认信息（非空），不应该被抑制。
  </action>
  <verify>
    <automated>cd /root/workspace/feishu-backend && /opt/apache-maven-3.9.5/bin/mvn test -pl feishu-bot-app -Dtest=BotMessageAppServiceTest -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - BotMessageAppService.sendReply() 对 null 和空字符串/空白字符串均跳过回复
    - 所有现有 BotMessageAppServiceTest 通过
    - 新增 2 个空字符串防护测试通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: status 命令 + 白名单完善 + 群聊引导 (CMD-01, CMD-02, COMPAT-02)</name>
  <files>
    feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java,
    feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeApp.java,
    feishu-bot-domain/src/main/java/com/qdw/feishu/domain/topic/TopicCommandValidator.java,
    feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandlerTest.java,
    feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/OpenCodeAppTest.java
  </files>
  <behavior>
    - Test: /oc status 在 INITIALIZED 状态返回当前会话信息（与 session status 相同）
    - Test: /oc status 在 UNINITIALIZED 状态返回"未绑定"提示
    - Test: /oc status 在 NON_TOPIC 状态被白名单拦截
    - Test: 白名单 NON_TOPIC 包含 "new"（COMPAT-02 需要 chatnow/new 在群聊可用）
    - Test: 白名单 UNINITIALIZED 包含 "new" 和 "status"
    - Test: TopicCommandValidator NON_TOPIC 拦截消息包含 "请在话题中操作" 和 "先发送 /oc cn"
    - Test: chat 命令在 NON_TOPIC 被白名单拦截时，错误信息包含群聊引导
  </behavior>
  <action>
    **Per D-02 (CMD-01): 新增 status 快捷命令**

    1. **OpenCodeCommandHandler.handle()** — 在 switch-case 中添加 `case "status"`：
       ```java
       case "status" -> AppExecutionResult.text(sessionManager.getCurrentSessionStatus(messageContext));
       ```
       放在 `case "session", "sc"` 之前。`status` 是 `session status` 的快捷方式。

    2. **OpenCodeApp.getCommandWhitelist()** — 更新白名单：
       - **NON_TOPIC**: 添加 `"new"`（COMPAT-02：允许在群聊中 `new <项目> <内容>` 创建话题）。注意 `"status"` 不加入 NON_TOPIC（status 需要话题上下文）。
       - **UNINITIALIZED**: 添加 `"status"` 和 `"new"`。
       - **INITIALIZED**: 已经是 `CommandWhitelist.all()`，无需改动。
       
       最终白名单：
       ```
       NON_TOPIC: help, connect, projects, p, sessions, s, session, sc, chatnow, cn, new
       UNINITIALIZED: help, connect, projects, p, sessions, s, session, sc, reset, commands, chatnow, cn, new, status
       INITIALIZED: all
       ```

    3. **OpenCodeApp.getHelp()** — 添加 `status` 命令到帮助文本的"会话管理"区域：
       ```
       `/opencode status`             - 快速查看当前绑定状态
       ```

    **Per COMPAT-02: 群聊引导消息增强**

    4. **TopicCommandValidator.buildRestrictedMessage()** — 优化 `NON_TOPIC` 状态的拦截消息：
       将当前的通用消息改为更具体的引导：
       ```
       ⚠️ 命令 `chat` 需要在话题中使用
       
       💡 在群聊中，你可以：
        - 使用 `/oc cn <问题>` 快速创建话题并对话（推荐）
        - 使用 `/oc new <项目> <问题>` 在指定项目创建话题
        - 或进入已有话题后直接输入问题
       ```
       确保消息中包含 "请在话题中操作" 语义和具体的 `/oc cn` 示例。

    5. **测试** — 更新/新增测试：
       - 在 `OpenCodeCommandHandlerTest` 中新增：
         - `should_returnSessionStatus_when_statusCommandInInitializedTopic` — INITIALIZED 状态下 `/oc status` 返回会话信息
         - `should_returnUnboundHint_when_statusCommandInUninitializedTopic` — UNINITIALIZED 状态下 `/oc status` 返回引导
       - 在 `OpenCodeAppTest` 中：
         - 验证 `getCommandWhitelist(NON_TOPIC)` 包含 "new"
         - 验证 `getCommandWhitelist(UNINITIALIZED)` 包含 "status" 和 "new"
       
    **别名完整性检查（CMD-02）**：
    审查当前白名单，确认所有别名对已包含。当前已有：`p`↔`projects`, `s`↔`sessions`, `cn`↔`chatnow`。检查 `session` 和 `sc` 是否在所有需要的状态中对齐。根据现有代码，`session` 和 `sc` 已同时出现在 NON_TOPIC 和 UNINITIALIZED 白名单中 ✓。
  </action>
  <verify>
    <automated>cd /root/workspace/feishu-backend && /opt/apache-maven-3.9.5/bin/mvn test -pl feishu-bot-domain -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    - `/oc status` 在 INITIALIZED 话题中返回绑定状态信息
    - `/oc status` 在 UNINITIALIZED 话题中返回未绑定引导
    - `/oc status` 在 NON_TOPIC 被白名单拦截
    - 白名单 NON_TOPIC 包含 new, chatnow, cn
    - 白名单 UNINITIALIZED 包含 status, new
    - NON_TOPIC 拦截消息包含 `/oc cn` 引导示例
    - 所有 280 个现有测试继续通过
    - 新增 4+ 个测试覆盖 status 命令和白名单完整性
  </done>
</task>

</tasks>

<verification>
# 全量测试
cd /root/workspace/feishu-backend && /opt/apache-maven-3.9.5/bin/mvn test -q 2>&1 | tail -10

# 空气泡防护确认
grep -n "replyContent.trim().isEmpty()" feishu-bot-app/src/main/java/com/qdw/feishu/app/message/BotMessageAppService.java

# status 命令路由确认
grep -n 'case "status"' feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java

# 白名单 new 命令确认
grep -n '"new"' feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeApp.java
</verification>

<success_criteria>
1. 异步任务路径（executeTask）不产生空消息气泡 — BotMessageAppService 防护空字符串
2. `/oc status` 快捷命令正常工作（INITIALIZED 返回状态，UNINITIALIZED 返回引导，NON_TOPIC 被拦截）
3. 白名单完整包含别名：NON_TOPIC 含 new, UNINITIALIZED 含 status + new
4. 群聊非话题拦截消息包含具体引导（/oc cn 示例）
5. 全部 280+ 测试通过，新增测试覆盖以上场景
</success_criteria>

<output>
After completion, create `.planning/phases/02-command-router-conversation-ux/02-01-SUMMARY.md`
</output>
