---
phase: 03-cards-guided-flows
plan: "02"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java
  - feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/WizardManager.java
  - feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandlerTest.java
  - feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/WizardManagerTest.java
autonomous: true
requirements: [CARD-02]
gap_closure: true

must_haves:
  truths:
    - "用户在未绑定话题（UNINITIALIZED）中发送任意非管理命令，机器人自动弹出步骤1项目选择卡片"
    - "WizardManager 状态转换代码与 Javadoc 描述的并发策略一致（无虚假承诺）"
    - "457 个现有测试继续通过（回归无破坏）"
  artifacts:
    - path: "feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java"
      provides: "UNINITIALIZED 自动向导触发逻辑"
      contains: "wizardManager.start(chatId, topicId)"
    - path: "feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/WizardManager.java"
      provides: "并发安全的原子状态转换或准确的 Javadoc"
      contains: "activeWizards.compute"
    - path: "feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandlerTest.java"
      provides: "自动向导触发的自动化验证"
  key_links:
    - from: "OpenCodeCommandHandler.handle()"
      to: "wizardManager.start(chatId, topicId)"
      via: "UNINITIALIZED + !isExplicitControlCommand(subCommand) 守卫"
      pattern: "wizardManager\\.start\\(chatId, topicId\\)"
    - from: "WizardManager.handleSelectProject/Session/Confirm"
      to: "activeWizards.compute(topicId, ...)"
      via: "ConcurrentHashMap.compute() 原子操作"
      pattern: "activeWizards\\.compute\\("
---

<objective>
关闭 Phase 3 验证失败的两个 gap：
1. **CARD-02 自动触发缺失（Blocker）** — `wizardManager.start()` 从未在 UNINITIALIZED 流程中被自动调用。需在 `OpenCodeCommandHandler.handle()` 中，当话题处于 UNINITIALIZED 状态且用户未指定显式管理命令时，自动触发向导并直接发送步骤1卡片（`noReply()`）。
2. **compute() 并发承诺未兑现（Warning）** — `WizardManager` Javadoc 声称使用 `ConcurrentHashMap.compute()` 保证原子性，实际实现用 `volatile` 字段直接赋值。需要：要么用 `compute()` 实现真正的原子步骤转换，要么修正 Javadoc 准确描述 `volatile` 并发策略。

Purpose: 完成 CARD-02 需求中"首次使用时提供分步向导"的自动弹出行为，并消除代码与注释的不一致。
Output: 修改后的 `OpenCodeCommandHandler.java`（含自动触发逻辑 + 新测试）和修复后的 `WizardManager.java`（compute() 实现）。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cards-guided-flows/03-SUMMARY.md
@.planning/phases/03-cards-guided-flows/03-VERIFICATION.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From feishu-bot-domain/.../opencode/OpenCodeCommandHandler.java (行号参考):
```java
// handle() 入口 — 在 whitelist 校验通过后，向导拦截块之前/之后可插入自动触发
public AppExecutionResult handle(Message message, String subCommand, String[] parts,
                                  CommandWhitelist whitelist, MessageContext messageContext) {
    TopicState state = sessionManager.detectTopicState(messageContext);
    // 1. whitelist 校验 (L82-88)
    // 2. 向导拦截（向导进行中拒绝非向导命令，L92-101）
    // 3. switch(subCommand) 路由 (L104-122)
}

// 已有的辅助方法 — 可复用模式参考 trySendSessionListCard() + handleWizardAction()
private AppExecutionResult handleWizardAction(String action, Message message, MessageContext ctx) {
    // L461-497: feishuGateway.sendInteractiveMessage(message, cardJson, topicId) + return noReply()
}
```

From feishu-bot-domain/.../opencode/WizardManager.java:
```java
// start() — 构建项目选择卡片并返回 WizardResult
public WizardResult start(String chatId, String topicId) { ... }

// WizardResult.getCardContent() 返回 CardContent，需由调用方 render + send
public CardContent getCardContent() { ... }

// handleSelectProject/Session/Confirm — 当前用 volatile 字段赋值，需改为 compute()
private WizardResult handleSelectProject(WizardState state, String project) {
    state.selectedProject = project;      // ← 需包装进 compute()
    state.step = WizardStep.SELECT_SESSION;
    ...
}
```

From feishu-bot-domain/.../opencode/WizardManager.java — activeWizards:
```java
private final Map<String, WizardState> activeWizards = new ConcurrentHashMap<>();

// compute() 原子操作模式（目标实现）：
activeWizards.compute(topicId, (k, v) -> {
    if (v == null || v.isExpired(ttlMillis)) return v; // 无变化
    v.selectedProject = project;
    v.step = WizardStep.SELECT_SESSION;
    return v;
});
```

From 已有的 handleWizardAction() 调用链（L461-497），send card 模式：
```java
WizardManager.WizardResult wizardResult = wizardManager.handleAction(subCommand, chatId, topicId);
if (wizardResult.getCardContent() != null) {
    CardActionContext actionCtx = CardActionContext.from(messageContext);
    String cardJson = cardRenderer.render(wizardResult.getCardContent(), actionCtx);
    feishuGateway.sendInteractiveMessage(message, cardJson, topicId);
    return AppExecutionResult.noReply();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UNINITIALIZED 自动向导触发 — OpenCodeCommandHandler</name>
  <files>
    feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java
    feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandlerTest.java
  </files>
  <behavior>
    - Test 1: UNINITIALIZED + inTopic + 非管理命令 + 无活跃向导 → `wizardManager.start()` 被调用，`feishuGateway.sendInteractiveMessage()` 被调用，返回 `noReply()`（`getReplyContent() == null`）
    - Test 2: UNINITIALIZED + inTopic + 明确管理命令 `sc`（显式控制）→ **不**触发向导，走正常路由
    - Test 3: UNINITIALIZED + inTopic + 向导已活跃 → 向导拦截正常触发，**不**重复调用 `wizardManager.start()`
    - Test 4: INITIALIZED + inTopic + 非管理命令 → **不**触发向导（仅 UNINITIALIZED 触发）
    - Test 5: UNINITIALIZED + NON_TOPIC（topicId = null）→ **不**触发向导（只在话题内触发）
  </behavior>
  <action>
在 `handle()` 方法中，向导拦截块（`isWizardActive` 检查，大约 L92-101）**之后**，`switch(subCommand)` 路由**之前**，插入自动触发逻辑：

```java
// 自动向导触发：UNINITIALIZED + 在话题内 + 无活跃向导 + 非显式管理命令
if (inTopic && state == TopicState.UNINITIALIZED
        && wizardManager != null && !wizardManager.isWizardActive(topicId)
        && !isExplicitControlCommand(subCommand)) {
    log.info("UNINITIALIZED 话题自动触发向导: topicId={}, subCommand={}", topicId, subCommand);
    String chatId = message.getChatId();
    WizardManager.WizardResult wizardResult = wizardManager.start(chatId, topicId);
    if (wizardResult.getCardContent() != null) {
        CardActionContext actionCtx = CardActionContext.from(messageContext);
        String cardJson = cardRenderer.render(wizardResult.getCardContent(), actionCtx);
        feishuGateway.sendInteractiveMessage(message, cardJson, topicId);
        return AppExecutionResult.noReply();
    }
}
```

在同一类中添加私有辅助方法（区分"显式管理命令"——用户有明确意图，无需弹向导）：

```java
/**
 * 显式管理命令：用户有明确操作意图，跳过自动向导触发。
 * 包含所有在 UNINITIALIZED 白名单中有具体功能意义的命令。
 */
private boolean isExplicitControlCommand(String subCommand) {
    return switch (subCommand) {
        case "connect", "help", "status", "projects", "p",
             "sessions", "s", "session", "sc",
             "chatnow", "cn", "new", "reset", "commands" -> true;
        default -> subCommand.startsWith("wizard_");
    };
}
```

**注意**：
- `isWizardAction()` 已存在，`isExplicitControlCommand()` 是新方法，两者不冲突
- `CardActionContext.from(messageContext)` 与 `handleWizardAction()` 中的模式一致（per D-17）
- 不需要修改 `OpenCodeApp.execute()` — 触发点在 handler 层更合适（handler 已有 state + wizard 全部依赖）
- 测试中 `wizardManager.start()` mock 返回值需要是一个含 CardContent 的 WizardResult：
  ```java
  CardContent mockCard = CardContent.builder().headerTitle("Test").build();
  WizardManager.WizardResult mockResult = WizardManager.WizardResult.of(mockCard, WizardManager.WizardStep.SELECT_PROJECT);
  when(wizardManager.start(anyString(), eq(topicId))).thenReturn(mockResult);
  when(cardRenderer.render(any(), any())).thenReturn("{\"schema\":\"2.0\"}");
  ```
  </action>
  <verify>
    <automated>mvn test -pl feishu-bot-domain -Dtest=OpenCodeCommandHandlerTest -q 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `OpenCodeCommandHandlerTest` 全部通过（含新增5个自动触发测试）
    - UNINITIALIZED + 非管理命令 → `wizardManager.start()` 被 `verify()` 确认调用
    - UNINITIALIZED + 显式管理命令（如 `sc`）→ `wizardManager.start()` **不被调用**（verify(wizardManager, never()).start(...)）
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WizardManager compute() 原子状态转换</name>
  <files>
    feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/WizardManager.java
    feishu-bot-domain/src/test/java/com/qdw/feishu/domain/opencode/WizardManagerTest.java
  </files>
  <behavior>
    - Test 1（现有，需保持通过）: `handleAction("wizard_select_project:proj", ...)` → 返回 SELECT_SESSION 步骤卡片
    - Test 2（现有，需保持通过）: 完整 3 步流程 start → select_project → select_session → confirm → COMPLETED
    - Test 3（新增）: 并发测试 — 两个线程同时调用 `handleAction("wizard_select_project:proj", ...)` → 最终只产生一次有效步骤转换（`WizardStep.step == SELECT_SESSION`），不出现竞态导致步骤回滚
    - Test 4（文档一致性）: 修复 Javadoc L29 — 去除"使用 compute() 保证原子性"旧表述，改为准确描述 `compute()` 实际使用情况
  </behavior>
  <action>
将 `handleSelectProject`、`handleSelectSession`、`handleConfirm` 内的状态修改包装进 `activeWizards.compute(topicId, ...)` 原子块中，消除读-改-写竞态窗口。

**handleSelectProject 改造（参考实现）**：
```java
private WizardResult handleSelectProject(WizardState state, String project) {
    // 用 compute() 原子完成读-改-写
    activeWizards.compute(state.topicId, (k, v) -> {
        if (v == null || v.isExpired(ttlMillis)) return v;
        v.selectedProject = project;
        v.step = WizardStep.SELECT_SESSION;
        return v;
    });
    log.info("向导步骤1完成，选择项目: topicId={}, project={}", state.topicId, project);
    // 以下卡片构建逻辑不变（纯读，不需要在 compute 内）
    String sessionsText = openCodeGateway.listRecentSessions(project, 10);
    ...
}
```

**同样方式改造** `handleSelectSession` 和 `handleConfirm`（对应各自的字段修改）。

**更新 Javadoc（L29 类注释）**：将"状态转换使用 `ConcurrentHashMap#compute` 保证原子性"描述改为实际情况（如果 compute() 实现了，则验证描述准确；如果因方法签名复杂度选择只改 Javadoc，则明确说明实际策略）。

**注意**：
- `WizardState` 字段 `selectedProject`、`selectedSessionId`、`step` 保持 `volatile` — 读路径（`isWizardActive`、`handleAction` 中的状态检查）仍需可见性保证
- `handleAction()` 方法整体已在 `activeWizards.get(topicId)` 后拿到 state 引用，在 `compute()` 内 state 和 map entry 是同一对象，安全
- 卡片构建（`CardContent.builder()`调用、`openCodeGateway.listRecentSessions()`）不需要在 compute 内 — IO操作不应在 compute 内执行（会阻塞整个 map segment）
  </action>
  <verify>
    <automated>mvn test -pl feishu-bot-domain -Dtest=WizardManagerTest -q 2>&amp;1 | tail -10</automated>
  </verify>
  <done>
    - `WizardManagerTest` 全部通过（含新并发测试）
    - `WizardManager.java` 中 `handleSelectProject`/`handleSelectSession`/`handleConfirm` 的字段修改均包含在 `activeWizards.compute(...)` 调用内
    - Javadoc 与实现一致（无虚假 compute() 声明 或 compute() 已实装）
  </done>
</task>

<task type="auto">
  <name>Task 3: 全量回归 + APP_USAGE_GUIDE 文档同步</name>
  <files>
    APP_USAGE_GUIDE.md
  </files>
  <action>
**步骤1 — 全量构建验证**：
```bash
mvn clean test -q 2>&1 | tail -20
```
确认 ≥457 测试通过，无新增失败。

**步骤2 — 手动检查 OpenCodeApp 白名单**：
确认 `OpenCodeApp.getCommandWhitelist(UNINITIALIZED)` 中包含 `wizard_*` action 白名单（Task 1 的自动触发会 bypass 白名单校验，但向导过程中的卡片点击仍需通过白名单）。如有遗漏，补充到白名单中。

**步骤3 — 更新 APP_USAGE_GUIDE.md**：
在 OpenCodeApp 章节的向导说明中，更新"触发方式"描述：
- 将"向导需要手动触发（`/oc connect`）"改为"**向导自动弹出**：在未绑定话题中发送任意消息（非 `/oc sc`、`/oc cn` 等管理命令），机器人自动弹出项目选择卡片"
- 保留手动 `/oc connect` 触发方式的说明（作为备选路径）
  </action>
  <verify>
    <automated>mvn clean test -q 2>&amp;1 | grep -E "Tests run:|BUILD" | tail -5</automated>
  </verify>
  <done>
    - 全量构建通过，测试数量 ≥457（含 Task 1+2 新增测试）
    - `APP_USAGE_GUIDE.md` 中向导触发说明准确反映自动弹出行为
    - git commit 包含代码+文档
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Domain 层测试（包含核心修改）
mvn test -pl feishu-bot-domain -q 2>&1 | tail -5

# 2. 全量回归
mvn clean test -q 2>&1 | grep -E "Tests run:|BUILD" | tail -5

# 3. 自动触发关键代码存在
grep -n "wizardManager.start(chatId, topicId)" \
  feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/OpenCodeCommandHandler.java

# 4. compute() 真实使用
grep -n "activeWizards.compute" \
  feishu-bot-domain/src/main/java/com/qdw/feishu/domain/opencode/WizardManager.java
```
</verification>

<success_criteria>
1. **CARD-02 gap 已关闭**: `OpenCodeCommandHandler.handle()` 在 UNINITIALIZED + inTopic + 非显式命令 时自动调用 `wizardManager.start()` 并发送步骤1卡片（`noReply()`），有 ≥2 个自动化测试验证
2. **compute() 一致性**: `WizardManager` 的状态转换方法使用 `activeWizards.compute()` 实现原子步骤修改，Javadoc 与代码一致
3. **回归不破坏**: ≥457 测试通过，无新失败
4. **文档同步**: `APP_USAGE_GUIDE.md` 更新向导自动触发说明
</success_criteria>

<output>
After completion, create `.planning/phases/03-cards-guided-flows/03-02-SUMMARY.md` with:
- What was fixed (gap 1 + gap 2 details)
- Files modified + commit hashes
- Test counts (before / after)
- Deviations if any
</output>
