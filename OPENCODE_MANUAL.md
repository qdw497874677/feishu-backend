# OpenCode 飞书助手 — 操作手册

> 通过飞书对话控制 OpenCode AI 代码助手，支持多轮对话、会话管理和交互式向导。
>
> 命令前缀：`/opencode`、`/oc`、`/code`（本手册统一使用最短写法 `/oc`）

---

## 快速上手（30秒入门）

以下 3 种方式，任选其一即可开始：

**方式一：立即对话（推荐，零配置）**

```
/oc cn 帮我写个排序函数
```

系统自动创建会话、绑定话题、返回结果。开箱即用。

**方式二：在话题中直接提问（已绑定会话时）**

```
帮我优化这个函数
```

话题已绑定会话后，输入任何不以 `/` 开头的文字都会自动发送给 AI，无需命令前缀。

**方式三：选一个已有会话绑定后对话**

```
/oc p                  → 查看项目
/oc s feishu-backend   → 查看会话列表
/oc sc ses_abc123      → 绑定会话
帮我重构登录模块        → 直接对话
```

---

## 命令速查表

| 命令 | 别名 | 说明 | 话题外 | 未初始化 | 已初始化 |
|------|------|------|:------:|:--------:|:--------:|
| `/oc chatnow <内容>` | `cn` | 立即对话（自动创建并绑定会话） | ✅ | ✅ | ✅ |
| `/oc chat <内容>` | — | 继续对话（话题内） | ❌ | ❌ | ✅ |
| `/oc new <提示词>` | — | 在当前项目创建新会话 | ❌ | ❌ | ✅ |
| `/oc new <项目> <提示词>` | — | 在指定项目创建新会话 | ✅ | ✅ | ✅ |
| `/oc projects` | `p` | 查看可用项目列表 | ✅ | ✅ | ✅ |
| `/oc sessions <项目>` | `s` | 查看项目的最近会话 | ✅ | ✅ | ✅ |
| `/oc sc <会话ID>` | — | 绑定会话到当前话题 | ✅ | ✅ | ✅ |
| `/oc session status` | — | 查看当前会话信息 | ❌ | ✅ | ✅ |
| `/oc session list` | — | 查看所有会话 | ❌ | ✅ | ✅ |
| `/oc session continue <ID>` | — | 继续指定会话（等价 `sc`） | ❌ | ✅ | ✅ |
| `/oc status` | — | 快速查看绑定状态 | ❌ | ✅ | ✅ |
| `/oc connect` | — | 检查服务连接状态 | ✅ | ✅ | ✅ |
| `/oc reset` | — | 重置话题（解除绑定） | ❌ | ✅ | ✅ |
| `/oc commands` | — | 查看所有可用斜杠命令 | ❌ | ✅ | ✅ |
| `/oc help` | — | 显示完整帮助信息 | ✅ | ✅ | ✅ |

> **应用前缀别名**：`/opencode` = `/oc` = `/code`，所有命令前缀通用，本手册以 `/oc` 为例。

---

## 核心使用流程

### 流程一：首次使用（向导引导）

当你第一次在一个飞书**话题**中发送任何 OpenCode 命令（非显式管理命令如 `help`、`projects`）时，系统会自动弹出 **3 步交互式向导卡片**，引导你完成会话绑定。

**Step 1 — 选择项目**

```
╔══════════════════════════════════════════════╗
║  🎯 欢迎使用 OpenCode！第 1 步：选择项目      ║
║                                              ║
║  请选择一个项目开始：                          ║
║                                              ║
║  [ feishu-backend ]  [ other-project ]        ║
║                                              ║
║  [ ❌ 取消 ]                                  ║
╚══════════════════════════════════════════════╝
```

点击一个项目按钮。

**Step 2 — 选择会话**

```
╔══════════════════════════════════════════════╗
║  📋 第 2 步：选择会话                         ║
║                                              ║
║  项目 feishu-backend 的最近会话：              ║
║                                              ║
║  [ 重构登录模块 — 2小时前 ]                    ║
║  [ 添加单元测试 — 1天前 ]                      ║
║  [ + 新建会话 ]   [ ❌ 取消 ]                  ║
╚══════════════════════════════════════════════╝
```

点击一个已有会话，或点击「新建会话」。

**Step 3 — 确认绑定**

```
╔══════════════════════════════════════════════╗
║  ✅ 第 3 步：确认绑定                         ║
║                                              ║
║  确认将以下会话绑定到当前话题？                 ║
║                                              ║
║  📁 项目: feishu-backend                      ║
║  🆔 会话: ses_abc123                          ║
║                                              ║
║  [ ✅ 确认绑定 ]   [ ❌ 取消 ]                 ║
╚══════════════════════════════════════════════╝
```

点击「确认绑定」，即完成！之后在话题中直接输入文字就能对话。

> ⚠️ **注意事项**
> - 向导进行中，其他命令会被拦截，需先完成或取消向导
> - 取消向导：点击卡片上的「❌ 取消」按钮，或输入 `/oc wizard_cancel`
> - 向导 **10 分钟后自动超时**，超时后需重新触发

---

### 流程二：快速对话（chatnow）

最简单的使用方式，**无需任何配置**，一条命令搞定：

```
用户输入：
/oc cn 帮我写个排序函数

机器人回复：
📎 opencode | ses_abc123

✅ 会话已创建并绑定到话题

📋 会话信息
  🆔 Session ID: ses_abc123
  💬 话题 ID: topic_xxx
  ✅ 状态: 已绑定

💡 开始对话
  在当前话题中发送：
  chat <你的问题>
  或直接输入问题

📁 默认工作目录
  /root/workspace/feishu-backend/workspace/2026-04-26/
```

系统自动完成三件事：
1. 在 `项目启动目录/workspace/当天日期/` 下创建新会话
2. 将会话绑定到当前话题
3. 返回会话基础信息

之后直接输入文字即可继续对话：

```
用户输入：
再优化一下性能

机器人回复：
📎 opencode | ses_abc123

[AI 回复性能优化建议...]
```

---

### 流程三：绑定现有会话后对话

适合已有历史会话、想继续之前工作的场景。

**Step 1 — 查看项目**

```
用户输入：
/oc p

机器人回复：
📎 opencode | 未绑定会话

🔗 OpenCode 连接成功

**服务状态**
✓ 服务运行正常

**📁 可用项目**
- feishu-backend
- other-project

---
💡 下一步：`/oc sessions <项目名>` 查看会话列表
```

**Step 2 — 查看会话**

```
用户输入：
/oc s feishu-backend

机器人回复：
📋 [交互式卡片] feishu-backend 的最近会话

  [ 重构登录模块 — 2小时前 ]
  [ 添加单元测试 — 1天前 ]
  [ + 新建会话 ]
```

> 💡 在话题中执行 sessions 命令会优先显示**交互式卡片**，点击按钮可直接绑定，无需手动输入会话 ID。卡片发送失败时自动降级为纯文本。

**Step 3 — 绑定会话**

```
用户输入：
/oc sc ses_abc123

机器人回复：
✅ 会话已绑定

当前话题已连接到会话：ses_abc123

---
💡 下一步：直接输入问题开始对话，或 `/oc chat <内容>`
```

**Step 4 — 开始对话**

```
用户输入：
/oc chat 帮我重构登录模块

机器人回复：
📎 opencode | ses_abc123

[AI 回复重构建议...]
```

之后可直接输入文字继续多轮对话：

```
用户输入：
再加上异常处理

机器人回复：
📎 opencode | ses_abc123

[AI 回复异常处理代码...]
```

---

## 话题状态说明

OpenCode 根据飞书话题的绑定情况，将使用状态分为三种：

| 状态 | 含义 | 典型场景 |
|------|------|---------|
| **话题外** | 不在飞书话题中（私聊或群聊非话题消息） | 直接发消息给机器人，没有 topicId |
| **话题未初始化** | 在话题中，但尚未绑定 OpenCode 会话 | 新建话题后首次使用 |
| **话题已初始化** | 在话题中，且已绑定 OpenCode 会话 | 完成绑定后的正常使用状态 |

### 各状态允许的操作

**话题外**

| 可用 | 不可用 |
|------|--------|
| `/oc chatnow/cn` | `/oc chat` |
| `/oc new <项目> <内容>` | `/oc new <内容>` |
| `/oc projects/p` | `/oc status` |
| `/oc sessions/s` | `/oc reset` |
| `/oc sc <ID>` | `/oc commands` |
| `/oc connect` | |
| `/oc help` | |

**话题未初始化**

| 可用 | 不可用 |
|------|--------|
| 上述话题外的全部命令 | `/oc chat` |
| `/oc status` | |
| `/oc reset` | |
| `/oc commands` | |
| `/oc new <内容>`（话题外不可用，此处不可用） | |

> 实际上话题未初始化时，推荐使用 `/oc chatnow` 一键创建并绑定，或等待向导自动弹出。

**话题已初始化**

所有命令均可用。可以直接输入文字（无需 `/oc chat` 前缀）进行对话。

---

## 命令详解

### `chatnow` / `cn` — 立即对话

**格式**

```
/oc chatnow <你想说的话>
/oc cn <你想说的话>
```

**说明**

这是最推荐的对话方式。一条命令完成所有事情：创建新会话 → 绑定到话题 → 发送消息 → 返回结果。

**权限**：所有状态可用（话题外、未初始化、已初始化）

**示例**

```
用户输入：
/oc cn 帮我写一个冒泡排序

机器人回复：
📎 opencode | ses_new123

✅ 会话已创建并绑定到话题

📋 会话信息
  🆔 Session ID: ses_new123
  💬 话题 ID: topic_xxx
  ✅ 状态: 已绑定
```

**注意事项**
- 如果话题已绑定会话，`chatnow` 会提示当前已绑定，不会覆盖
- 自动创建的工作目录为 `项目启动目录/workspace/YYYY-MM-DD/`

---

### `chat` — 继续对话

**格式**

```
/oc chat <你想说的话>
```

**说明**

在已绑定会话的话题中继续对话。如果话题已初始化，也可以直接输入文字（无需命令前缀）。

**权限**：仅话题已初始化时可用

**示例**

```
用户输入：
/oc chat 添加单元测试

机器人回复：
📎 opencode | ses_abc123

[AI 回复单元测试代码...]
```

或直接输入（等价效果）：

```
用户输入：
添加单元测试

机器人回复：
📎 opencode | ses_abc123

[AI 回复单元测试代码...]
```

**注意事项**
- 话题未绑定时使用此命令会提示需要先初始化
- 如果 `chat` 后没有内容，会显示当前会话信息和用法提示

---

### `new` — 创建新会话

**格式**

```
# 话题已绑定时（使用当前项目）
/oc new <提示词>

# 所有场景（指定项目）
/oc new <项目名称> <提示词>
```

**说明**

创建一个新的 OpenCode 会话并自动绑定到当前话题。适用于想在同项目中开启新话题对话的场景。

**权限**
- `/oc new <提示词>` — 仅话题已初始化时可用
- `/oc new <项目> <提示词>` — 所有状态可用

**示例**

```
# 场景1：话题已绑定，在当前项目创建新会话
用户输入：
/oc new 优化算法

机器人回复：
📎 opencode | ses_new456

[创建新会话并开始对话...]

# 场景2：指定项目创建新会话
用户输入：
/oc new feishu-backend 重构登录模块

机器人回复：
📎 opencode | ses_new789

[在指定项目创建会话并开始对话...]
```

**注意事项**
- 话题未绑定时，`/oc new <提示词>`（不指定项目）会报错，必须使用 `/oc new <项目名> <提示词>`
- 创建新会话后，话题自动绑定到新会话（旧绑定会被替换）

---

### `projects` / `p` — 查看项目列表

**格式**

```
/oc projects
/oc p
```

**说明**

查看 OpenCode 服务上所有可用的项目。

**权限**：所有状态可用

**示例**

```
用户输入：
/oc p

机器人回复：
🔗 OpenCode 连接成功

**服务状态**
✓ 服务运行正常

**📁 可用项目**
- feishu-backend
- other-project

---
💡 下一步：`/oc sessions <项目名>` 查看会话列表
```

---

### `sessions` / `s` — 查看项目会话

**格式**

```
/oc sessions <项目名称>
/oc s <项目名称>
```

**说明**

查看指定项目的最近会话列表。在话题中执行时优先显示交互式卡片（可点击按钮直接绑定），非话题环境显示纯文本。

**权限**：所有状态可用

**示例**

```
用户输入：
/oc s feishu-backend

机器人回复：
📋 [交互式卡片] feishu-backend 的最近会话

  [ 重构登录 — 2小时前 ]    ← 点击直接绑定
  [ 添加测试 — 1天前 ]       ← 点击直接绑定
  [ + 新建会话 ]

---
💡 下一步：`/oc sc <会话ID>` 绑定会话到当前话题
```

**注意事项**
- 必须指定项目名称，否则会提示用法
- 项目名称区分大小写，需与 `projects` 命令显示的名称一致
- 卡片按钮标签过长时会截断显示

---

### `sc` — 绑定会话

**格式**

```
/oc sc <会话ID>
```

**说明**

将指定的会话绑定到当前话题，等价于 `/oc session continue <会话ID>`。

**权限**：话题外、话题未初始化、话题已初始化均可用

**示例**

```
用户输入：
/oc sc ses_abc123

机器人回复：
✅ 会话已绑定

当前话题已连接到会话：ses_abc123

---
💡 下一步：直接输入问题开始对话，或 `/oc chat <内容>`
```

**注意事项**
- 不提供会话 ID 时会提示用法和示例
- 绑定成功后，话题中的纯文字消息会自动路由到该会话

---

### `session` — 会话管理

**格式**

```
# 快速查看绑定状态
/oc status

# 查看当前会话详情
/oc session status

# 查看所有会话
/oc session list

# 继续指定会话（等价 sc）
/oc session continue <会话ID>
```

**说明**

`session` 是一组会话管理子命令的总入口。`status` 是最常用的子命令，可快速确认当前绑定状态。

**权限**
- `/oc status` — 话题未初始化和已初始化可用
- `/oc session *` — 话题未初始化和已初始化可用

**示例**

```
用户输入：
/oc status

机器人回复：
📎 opencode | ses_abc123

💬 当前会话信息

  🆔 Session ID: ses_abc123
  💬 话题 ID: topic_xxx
  ✅ 状态: 已绑定
```

---

### `connect` — 检查连接状态

**格式**

```
/oc connect
```

**说明**

检查与 OpenCode 服务的连接状态，同时显示可用项目列表。常用于排查连接问题。

**权限**：所有状态可用

**示例**

```
用户输入：
/oc connect

机器人回复：
🔗 OpenCode 连接成功

**服务状态**
✓ 服务运行正常

**📁 可用项目**
- feishu-backend
- other-project

---
💡 下一步：`/oc projects` 查看项目列表
```

---

### `reset` — 重置话题

**格式**

```
/oc reset
```

**说明**

清除当前话题的会话绑定，回到「未初始化」状态，允许重新绑定其他会话。

**权限**：仅话题内可用（未初始化和已初始化均可）

**示例**

```
用户输入：
/oc reset

机器人回复：
🔄 话题已重置

已解除绑定的会话: ses_abc123

✅ 可以重新初始化了

**下一步操作**：

1️⃣ 查看可用项目：
   /oc p

2️⃣ 查看项目的最近会话：
   /oc s feishu-backend

3️⃣ 选择会话并绑定：
   /oc sc <会话ID>

---
💡 下一步：`/oc sc <会话ID>` 重新绑定，或 `/oc sessions` 查看会话
```

**注意事项**
- 只能在话题中使用，话题外执行会提示错误
- 重置后可使用 `/oc chatnow` 快速重新绑定，或等待向导自动弹出

---

### `commands` — 查看所有命令

**格式**

```
/oc commands
```

**说明**

查看 OpenCode 应用支持的所有斜杠命令列表。

**权限**：话题未初始化和已初始化可用

---

### `help` — 帮助信息

**格式**

```
/oc help
```

**说明**

显示 OpenCode 完整帮助信息，包含所有命令格式、使用场景和示例。

**权限**：所有状态可用

**注意事项**
- `help` 命令的回复不显示「📎 状态指示器」
- 也可以直接发送 `/oc`（无子命令）查看简短引导

---

## 常见场景 & 问题解答

### Q1：第一次用 OpenCode，怎么开始？

**推荐用 `chatnow`，一条命令搞定：**

```
/oc cn 你好，帮我写一段代码
```

系统自动创建会话、绑定话题、开始对话。之后直接输入文字就能继续聊。

---

### Q2：我不想创建新会话，想继续之前的会话怎么办？

**绑定已有会话：**

```
/oc s feishu-backend     # 查看会话列表
/oc sc ses_abc123        # 绑定到当前话题
```

如果在话题中，`sessions` 会显示交互式卡片，点击按钮即可直接绑定，无需手动输入 ID。

---

### Q3：怎么切换到另一个会话？

**方法一：重新绑定**

```
/oc reset                # 解除当前绑定
/oc sc ses_new456        # 绑定新会话
```

**方法二：直接覆盖绑定**

```
/oc sc ses_new456        # 直接绑定新会话（覆盖旧绑定）
```

---

### Q4：在话题里输入文字但没有回复 AI，怎么回事？

可能原因：
1. **话题未绑定会话** — 先执行 `/oc status` 检查绑定状态
2. **输入以 `/` 开头** — 以 `/` 开头的内容会被识别为命令，不会发送给 AI
3. **消息被去重** — 连续发送完全相同的内容可能被过滤

解决：先执行 `/oc status` 确认绑定状态，未绑定时用 `/oc cn <内容>` 快速开始。

---

### Q5：向导卡片弹出来后，我不想用了怎么关掉？

点击卡片上的「❌ 取消」按钮，或输入：

```
/oc wizard_cancel
```

向导会在 10 分钟后自动超时关闭。

---

### Q6：`chatnow` 和 `chat` 有什么区别？

| 命令 | 自动创建会话 | 自动绑定话题 | 需要先初始化 |
|------|:----------:|:----------:|:----------:|
| `chatnow` / `cn` | ✅ | ✅ | ❌ |
| `chat` | ❌ | ❌ | ✅（必须已绑定） |

简单说：`chatnow` 是从零开始，`chat` 是继续对话。

---

### Q7：命令发出去提示「话题未初始化」，怎么解决？

说明你在话题中但没有绑定会话。最简单的解决方式：

```
/oc cn 你想问的问题
```

或者等待向导卡片自动弹出，按步骤操作。

---

### Q8：如何查看当前绑定的是哪个会话？

```
/oc status
```

会显示当前绑定的 Session ID、话题 ID 和绑定状态。

---

### Q9：我想在另一个项目下创建新会话怎么做？

```
/oc new other-project 帮我写个接口
```

指定项目名称即可在任何状态下创建会话。

---

### Q10：OpenCode 服务连不上怎么办？

1. 执行 `/oc connect` 检查服务状态
2. 如果显示错误信息，联系管理员确认 OpenCode 服务是否正常运行
3. 服务恢复后，重新执行需要的命令即可

---

## 注意事项 & 限制

### 话题依赖

- 大部分功能（`chat`、`status`、`reset`、`commands`）需要在**飞书话题**中使用
- 不在话题中时，推荐使用 `/oc chatnow` 或 `/oc new <项目> <内容>` 来开始对话

### 向导限制

- 向导进行中，非向导命令会被拦截，必须先完成或取消向导
- 向导 **10 分钟自动超时**，超时后需要重新触发
- 向导只在「话题未初始化」状态下自动弹出

### 会话绑定

- 一个话题同时只能绑定一个会话
- 绑定新会话会覆盖旧绑定
- 使用 `/oc reset` 可以解除绑定，回到未初始化状态

### 工作目录

- `chatnow` 自动创建的会话，默认工作目录为 `项目启动目录/workspace/YYYY-MM-DD/`
- 会话的工作目录由 OpenCode 服务端决定

### 命令大小写

- 所有命令和别名**不区分大小写**：`/OC`、`/oc`、`/Oc` 效果相同
- 子命令也不区分大小写：`ChatNow`、`chatnow`、`CHATNOW` 效果相同

### 状态指示器

每条 OpenCode 回复头部会自动显示当前绑定状态：

```
📎 opencode | ses_abc123       ← 已绑定会话
📎 opencode | 未绑定会话        ← 未绑定
```

`help` 命令的回复不显示状态指示器。

### 下一步建议

执行完命令后，机器人会自动附加操作建议（对话类命令除外）：

```
[命令执行结果...]

---
💡 下一步：`/oc sessions <项目名>` 查看会话列表
```

### 消息路由

在已绑定会话的话题中：
- 不以 `/` 开头的纯文字 → 自动发送给 AI 对话
- 以 `/` 开头的内容 → 识别为命令

---

**文档版本**: 2026-04-26
